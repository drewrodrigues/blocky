// automock
// mock out getElementOrThrow && getElementsOrThrow

describe('listenForModalOpen', () => {
  // it checks for the modal every 100ms
  // it returns its interval
  //
  // when modal found:
  // - it inserts title
  // - it clicks calendar button
  // - it clicks calendar option
  // - it clicks save button
  // - it calls back isCreatingEvent with true
  // - it calls back isCreatingEvent with false once done
  //
  // it throws an error when unable to insert title
  // it throws an error when unable to click calendar button
  // it throws an error when unable to click calendar option
  // it throws an error when unable to click save button
})
